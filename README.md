A deep learning project that predicts medical insurance costs based on customer demographics using a neural network built with Keras.

Preprocessed customer data using label encoding and standard scaling

Built a neural network with Dense and Dropout layers

Trained and evaluated the model achieving ~90% accuracy

Libraries used, Python, Keras, scikit-learn, pandas, NumPy, Matplotlib, Seaborn
